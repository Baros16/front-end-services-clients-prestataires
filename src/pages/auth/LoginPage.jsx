// src/pages/auth/LoginPage.jsx
// TODO Semaine 2 — M2 + M5
import { useLocation } from "react-router-dom";
import PlaceHolderPage from "../_placeholder";

export default function LoginPage() {
  const { pathname } = useLocation();
  return (
    <PlaceHolderPage/>
  );
}
